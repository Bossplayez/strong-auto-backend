[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm i --legacy-peer-deps"]

[phases.build]
cmds = ["rm -rf dist tsconfig.build.tsbuildinfo", "npx prisma generate", "npm run build"]
